import Form from "./Form";
import Messages from "./Messages";

function ChatPannel() {
    return (
        <div>
            <Form />
            <Messages />
        </div>
    );
}

export default ChatPannel;
